spring.application.name=chat-box
# --------------------------------------------
# Server
# --------------------------------------------
# ??i port n?u c?n (m?c ??nh 8080)
server.port=8080

# Context path n?u mu?n (m?c ??nh tr?ng)
# server.servlet.context-path=/chat

# --------------------------------------------
# Logging
# --------------------------------------------
# Log m?c INFO cho toàn b? app
logging.level.root=INFO
# Log chi ti?t cho package c?a b?n
logging.level.com.example.chat=DEBUG
# ??nh d?ng log: th?i gian, level, thread, logger, message
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n

# --------------------------------------------
# Spring WebSocket / STOMP
# --------------------------------------------
# Không có property riêng cho STOMP broker trong config ??n gi?n này,
# vì mình ?ã c?u hình trong WebSocketConfig.java.
# N?u dùng broker th?t (RabbitMQ, ActiveMQ) thì m?i c?n property ? ?ây.

# --------------------------------------------
# Thymeleaf
# --------------------------------------------
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8

# --------------------------------------------
# HTTP encoding
# --------------------------------------------
# server.tomcat.uri-encoding=UTF-8

